openapi: 3.0.0
info:
  title: Postman API
  version: "1.0"
paths:
  /collections:
    get:
      summary: List collections
      responses:
        '200':
          description: OK
components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-KEY
servers:
  - url: https://api.postman.com
