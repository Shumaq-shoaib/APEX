openapi: "3.0.0"
info:
  title: Invalid API Spec
  version: "1.0.0"
paths:
  /users:
    get:
      summary: Get all users
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  users:
                    type: array
                    items:
                      type: string
components:
  schemas:
    User:        # ❌ Incorrect indentation breaks schema definition
    type: object
      properties:
        id:
          type: integer
        name:
          type: string
